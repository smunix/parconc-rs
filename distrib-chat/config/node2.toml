# Configuration for Cluster Node 2
# elfo-network clustering settings

[system.network]
listen = ["tcp://127.0.0.1:9302"]
discovery.predefined = ["tcp://127.0.0.1:9301"]
discovery.attempt_interval = "500ms"
compression.lz4 = "Preferred"
